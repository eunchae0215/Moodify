<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Moodify - 음악</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <%- include('../frame/header') %>

  <div class="music-page">
    <!-- 햄버거 메뉴 -->
    <button class="hamburger-menu" id="hamburgerMenu">
      <i class="fas fa-bars"></i>
    </button>

    <!-- 음악 플레이어 컨테이너 -->
    <div class="music-container">
      
      <!-- 배경: 고정된 재생 카드 -->
      <div class="player-card-fixed">
        <div class="player-card">
          <!-- 앨범 아트 (배경) -->
          <div class="album-art-bg" id="albumArtBg">
          </div>

          <!-- 노래 정보 -->
          <div class="song-info">
            <h2 class="song-title" id="songTitle">음악을 불러오는 중...</h2>
            <p class="song-artist" id="songArtist">잠시만 기다려주세요</p>
          </div>

          <!-- 재생 컨트롤 -->
          <div class="player-controls">
            <button class="control-btn prev-btn" id="prevBtn">
              <i class="fas fa-step-backward"></i>
            </button>
            <button class="control-btn play-btn" id="playBtn">
              <i class="fas fa-play"></i>
            </button>
            <button class="control-btn next-btn" id="nextBtn">
              <i class="fas fa-step-forward"></i>
            </button>
          </div>

          <!-- 재생바 -->
          <div class="progress-bar-section">
            <span class="time-text" id="currentTime">0:00</span>
            <div class="progress-bar-wrapper">
              <input 
                type="range" 
                class="progress-bar" 
                id="progressBar"
                min="0" 
                max="100" 
                value="0"
              >
              <div class="progress-fill" id="progressFill"></div>
            </div>
            <span class="time-text" id="totalTime">0:00</span>
          </div>
        </div>
      </div>

      <!-- 전면: 3D Carousel 썸네일 -->
      <div class="carousel-container">
        <div class="carousel-track" id="carouselTrack">
          <!-- JavaScript로 동적 생성됨 -->
        </div>
      </div>

      <!-- 음악 리스트 (오른쪽에서 슬라이드) -->
      <div class="list-section-overlay" id="listSectionOverlay">
        <!-- 닫기 화살표 버튼 -->
        <button class="close-list-btn" id="closeListBtn">
          <i class="fas fa-chevron-right"></i>
        </button>
        
        <div class="list-scroll-overlay">
          <!-- JavaScript로 동적 생성됨 -->
        </div>
      </div>

    </div>
  </div>

  <script src="/js/music_card.js"></script>
</body>
</html>
